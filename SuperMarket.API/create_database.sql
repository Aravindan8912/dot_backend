-- Run this in MySQL (e.g. mysql -u root -p < create_database.sql) before first run.
-- Creates the database that the API expects (MySQL may use lowercase name 'supermarketdb').
CREATE DATABASE IF NOT EXISTS SuperMarketDb
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;
