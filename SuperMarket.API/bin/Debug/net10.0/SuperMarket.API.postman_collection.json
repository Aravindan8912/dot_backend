{
  "info": {
    "name": "SuperMarket API",
    "description": "Use **Flow** folder to run: 1. Create Category → 2. Create Customer → 3. Create Product → 4. Place Order. Run in order; IDs are auto-saved for next step.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "variable": [
    { "key": "baseUrl", "value": "https://localhost:5001" },
    { "key": "categoryId", "value": "" },
    { "key": "customerId", "value": "" },
    { "key": "productId", "value": "" },
    { "key": "orderId", "value": "" }
  ],
  "item": [
    {
      "name": "Flow (run in order)",
      "description": "1. Create Category → 2. Create Customer → 3. Create Product → 4. Place Order. Run each request in order; response IDs are saved to variables for the next step.",
      "item": [
        {
          "name": "1. Create Category",
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "if (pm.response.code === 200) {",
                  "    const json = pm.response.json();",
                  "    if (json.id) pm.collectionVariables.set('categoryId', json.id);",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Groceries\"\n}"
            },
            "url": "{{baseUrl}}/api/categories",
            "description": "Step 1: Create a category. Response includes id → saved as categoryId for Create Product."
          }
        },
        {
          "name": "2. Create Customer",
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "if (pm.response.code === 200) {",
                  "    const json = pm.response.json();",
                  "    if (json.id) pm.collectionVariables.set('customerId', json.id);",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"John Doe\",\n  \"email\": \"john@example.com\",\n  \"phone\": \"+1234567890\"\n}"
            },
            "url": "{{baseUrl}}/api/customers",
            "description": "Step 2: Create a customer. Response includes id → saved as customerId for Place Order."
          }
        },
        {
          "name": "3. Create Product",
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "if (pm.response.code === 200) {",
                  "    const json = pm.response.json();",
                  "    if (json.id) pm.collectionVariables.set('productId', json.id);",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Milk 1L\",\n  \"price\": 2.99,\n  \"stock\": 100,\n  \"categoryId\": \"{{categoryId}}\"\n}"
            },
            "url": "{{baseUrl}}/api/products",
            "description": "Step 3: Create a product. Uses categoryId from step 1. Response id → saved as productId for Place Order."
          }
        },
        {
          "name": "4. Place Order",
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "if (pm.response.code === 200) {",
                  "    const json = pm.response.json();",
                  "    if (json && json.id) pm.collectionVariables.set('orderId', json.id);",
                  "}"
                ],
                "type": "text/javascript"
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"customerId\": \"{{customerId}}\",\n  \"items\": [\n    { \"productId\": \"{{productId}}\", \"quantity\": 2 }\n  ]\n}"
            },
            "url": "{{baseUrl}}/api/orders",
            "description": "Step 4: Place an order. Uses customerId from step 2 and productId from step 3."
          }
        }
      ]
    },
    {
      "name": "Categories",
      "item": [
        { "name": "Get all categories", "request": { "method": "GET", "url": "{{baseUrl}}/api/categories" }},
        { "name": "Get category by id", "request": { "method": "GET", "url": "{{baseUrl}}/api/categories/{{categoryId}}" }},
        {
          "name": "Create category",
          "request": {
            "method": "POST",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "body": { "mode": "raw", "raw": "{\n  \"name\": \"Groceries\"\n}" },
            "url": "{{baseUrl}}/api/categories"
          }
        }
      ]
    },
    {
      "name": "Customers",
      "item": [
        { "name": "Get all customers", "request": { "method": "GET", "url": "{{baseUrl}}/api/customers" }},
        { "name": "Get customer by id", "request": { "method": "GET", "url": "{{baseUrl}}/api/customers/{{customerId}}" }},
        {
          "name": "Create customer",
          "request": {
            "method": "POST",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "body": { "mode": "raw", "raw": "{\n  \"name\": \"John Doe\",\n  \"email\": \"john@example.com\",\n  \"phone\": \"+1234567890\"\n}" },
            "url": "{{baseUrl}}/api/customers"
          }
        }
      ]
    },
    {
      "name": "Products",
      "item": [
        { "name": "Get all products", "request": { "method": "GET", "url": "{{baseUrl}}/api/products" }},
        { "name": "Get product by id", "request": { "method": "GET", "url": "{{baseUrl}}/api/products/{{productId}}" }},
        {
          "name": "Create product",
          "request": {
            "method": "POST",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "body": { "mode": "raw", "raw": "{\n  \"name\": \"Milk 1L\",\n  \"price\": 2.99,\n  \"stock\": 100,\n  \"categoryId\": \"{{categoryId}}\"\n}" },
            "url": "{{baseUrl}}/api/products"
          }
        },
        {
          "name": "Update product",
          "request": {
            "method": "PUT",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "body": { "mode": "raw", "raw": "{\n  \"name\": \"Milk 1L (Updated)\",\n  \"price\": 3.49,\n  \"categoryId\": \"{{categoryId}}\"\n}" },
            "url": "{{baseUrl}}/api/products/{{productId}}"
          }
        }
      ]
    },
    {
      "name": "Orders",
      "item": [
        { "name": "Get all orders", "request": { "method": "GET", "url": "{{baseUrl}}/api/orders" }},
        { "name": "Get order by id", "request": { "method": "GET", "url": "{{baseUrl}}/api/orders/{{orderId}}" }},
        {
          "name": "Place order",
          "request": {
            "method": "POST",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "body": { "mode": "raw", "raw": "{\n  \"customerId\": \"{{customerId}}\",\n  \"items\": [\n    { \"productId\": \"{{productId}}\", \"quantity\": 2 }\n  ]\n}" },
            "url": "{{baseUrl}}/api/orders"
          }
        }
      ]
    }
  ]
}
